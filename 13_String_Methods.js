// String Methods

let my_name = "Abdul Rehman Kalsekar";
let text = "I am Abdul Rehman Kalsekar. This is JS Course."

// Gives you the Character at the Positions
console.log(my_name.charAt(4))

// Concatenates two Strings
console.log(my_name.concat(my_name, text))

// Gives you the index of the required characters
// console.log(text.indexOf('am'))

// Searches for the Index of required String
console.log(text.search('Abdul'))

// Prints the Character at required Indexs
console.log(my_name[2])

// Replaces the word
let a = text.replace('am', 'm');
console.log(a)


let indexes = text.split()
console.log(indexes)